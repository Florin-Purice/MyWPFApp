<Page x:Class="MyWPFApp.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MyWPFApp.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="DashboardPage"
      d:DataContext="{d:DesignInstance local:DashboardPage, IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d">
    <Grid VerticalAlignment="Center"
          HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Number A:" />
        <ui:NumberBox Grid.Row="0"
                      Grid.Column="1"
                      Width="200"
                      Margin="12,0,0,0"
                      Value="{Binding ViewModel.NumberA}" />
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="Number B:" />
        <ui:NumberBox Grid.Row="1"
                      Grid.Column="1"
                      Width="200"
                      Margin="12,0,0,0"
                      Value="{Binding ViewModel.NumberB}" />
        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Text="Sum:" />
        <TextBlock Grid.Row="2"
                   Grid.Column="1"
                   Width="200"
                   Margin="12,0,0,0"
                   Text="{Binding ViewModel.Sum}" />
        <ui:Button Grid.Row="3"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Command="{Binding ViewModel.CalculateSumCommand, Mode=OneWay}"
                   Content="Calculate Sum"
                   Icon="Fluent24" />
    </Grid>
</Page>
