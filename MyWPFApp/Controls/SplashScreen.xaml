<UserControl x:Class="MyWPFApp.Controls.SplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyWPFApp.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:SplashScreen, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource WindowBackground}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    >
            <ui:TextBlock Text="{Binding ViewModel.Message}"
                          Margin="20"/>
            <ui:ProgressRing IsIndeterminate="True" />
        </StackPanel>
    </Grid>
</UserControl>
